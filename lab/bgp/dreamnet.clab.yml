name: dreamnet-bgp

topology:
  groups:
    routers:
      kind: frr
      image: quay.io/frrouting/frr:containerlab-10.7.1
      extras:
        frr:
          daemons:
            - bgpd
  nodes:
    r1:
      group: routers
      startup-config: configs/r1/frr.conf
    r2:
      group: routers
      startup-config: configs/r2/frr.conf
    r3:
      group: routers
      startup-config: configs/r3/frr.conf
  links:
    - endpoints: ["r1:eth1", "r2:eth1"]
    - endpoints: ["r2:eth2", "r3:eth1"]
