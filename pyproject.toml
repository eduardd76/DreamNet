[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "dreamnet-rsi"
version = "0.2.0"
description = "An adaptive, evidence-driven troubleshooting procedure engine for network operations"
readme = "README.md"
requires-python = ">=3.11"
license = {text = "Apache-2.0"}
authors = [{name = "vExpertAI"}]
dependencies = []

[project.urls]
Homepage = "https://github.com/eduardd76/DreamNet"
Repository = "https://github.com/eduardd76/DreamNet"
Issues = "https://github.com/eduardd76/DreamNet/issues"

[project.optional-dependencies]
dev = ["pytest>=8.0", "ruff>=0.6"]

[project.scripts]
dreamnet = "dreamnet.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py311"
